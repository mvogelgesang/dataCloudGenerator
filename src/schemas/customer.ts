export interface Customer {
  id: string;
  loyaltyId: string;
  name: string;
  email: string;
  address: string;
  city: string;
  postalCode: string;
  country: string;
  createdAt: Date;
  modifiedAt: Date;
}